#include "RenderCompute0.hpp"

bool RenderCompute0::compute( std::vector<RenderContext> context, std::vector<RenderEntity> entities, std::vector<RenderLight> light, std::vector<RenderCamera> camera ){
    
}
