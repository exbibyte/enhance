#ifndef EN_TRACKER_COMPONENTS
#define EN_TRACKER_COMPONENTS

#include "enComponentMeta.h"

class enTrackerComponents : public enComponentMeta {
public:  
    enTrackerComponents : enComponentMeta( enComponentType::CLOCK ){
	
    }
};

#endif
