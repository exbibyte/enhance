MD5Version 10
commandline somecommandline

numJoints 20
numMeshes 30

joints {
name parentIndex ( vec3position ) ( vec3orientation )
}

mesh {
shader texture

numverts numVerts
vert vertexIndex ( vec2texCoords ) startWeight weightCount

numtris numTriangles
tri triangleIndex vertIndex0 vertIndex1 vertIndex2

numweights numWeights
weight weightIndex jointIndex floatweightBias ( vec3weightPosition )

}
